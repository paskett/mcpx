These extensions can improve the ergonomics of using `mcpx` from the `pi` agent harness.

`mcpx.ts` adds a *tool* to `pi` that simply falls back to the `mcpx` script on the host machine.

`mcpx-context.ts` adds a small blurb to the system prompt that tells the LLM which servers are available through `mcpx`.
